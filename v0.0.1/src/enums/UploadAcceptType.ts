enum UploadAcceptType {
  IMAGE = '.jpg, .png, .gif, .svg, .bmp, .webp',

  OFFICE = '.doc, .docx, .xls, .xlsx, .ppt, .pptx, .vsd, .mpp, .mppx, .mppx',

  ZIP = '.zip, .rar, .7zip',
}

export default UploadAcceptType;
