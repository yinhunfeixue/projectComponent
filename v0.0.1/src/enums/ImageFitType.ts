enum ImageFitType {
  // MOZ_INITIAL = "-moz-initial",
  // INHERIT = "inherit",
  // INITIAL = "initial",
  // REVERT = "revert",
  // UNSET = "unset",
  CONTAIN = "contain",
  COVER = "cover",
  // FILL = "fill",
  NONE = "none",
  // SCALE_DOWN = "scale-down",
}

export default ImageFitType