---
name: IdCardLink
route: /IdCardLink
order: -1
sidebar: true
---

import { Playground, Props } from 'docz';
import 'antd/dist/antd.css';
import {Form} from 'antd';
import IdCardLink from './IdCardLink';

# IdCardLink
根据输入的身份证号获取对应的个人信息


### 主要做了以下工作

 + 可以设定默认初始值
 + 可以根据返回的身份信息设置做对应的处理


### 你可以 

 + 设置默认需要获取信息的身份证
 

## 默认
<Playground>
  <IdCardLink getIdCardInfo={values => console.log(values)} />
</Playground>

## 初始化
<Playground>
  <IdCardLink getIdCardInfo={values => console.log(values)} value="420115199305126210"/>
</Playground>

## 属性
<Props of={IdCardLink} />

